# VECTRACOM — configuration Redis
bind 0.0.0.0
protected-mode no
port 6379
timeout 0
tcp-keepalive 300

# Persistance (cache + file de sync mobile + locks)
save 900 1
save 300 10
save 60 10000
appendonly yes
appendfilename "appendonly.aof"
dir /data

maxmemory 512mb
maxmemory-policy allkeys-lru
